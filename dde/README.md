`dd` tries to infer the language of every Lambda.
If it did so incorrectly, please include `$ tree` in the GitHub issue.

Do not do any cleaning in "do"! Might lead to actually more stuff lying around