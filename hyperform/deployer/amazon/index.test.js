describe('deployer', () => {
  describe('amazon', () => {
    test('one', () => {
      expect(1).toBe(1)

      // Command-line generators and runners (deploy, publish) will be replaced anyway with SDK calls
      // Do not write tests
    })
  })
})
