/**
 * Deploys functions anywhere in <dir> whose name fits <pickregex> as Lambda with the same name
 * @param {string} dir Root dir
 * @param {Regex} fnregex A regex that's tested with each function name
 */
function lift(dir, fnregex) {
  
}

module.exports = {
  lift,
}
